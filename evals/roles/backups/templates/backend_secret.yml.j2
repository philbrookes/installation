apiVersion: v1
data:
  AWS_S3_BUCKET_NAME: "{{ backups.aws.bucket_name | b64encode }}"
  AWS_ACCESS_KEY_ID: "{{ backups.aws.access_key_id | b64encode  }}"
  AWS_SECRET_ACCESS_KEY: "{{ backups.aws.access_key | b64encode  }}"
kind: Secret
metadata:
  name: "{{ eval_backups.backend_secret }}"
type: Opaque
